# kdk-wordpress プロジェクトのルール（Claude Code・Codex 共通）

このフォルダで作業するときは、`README.md` と `AGENTS.md` の安全ルールに加えて、次を必ず守る。

## 先生（宮部大輔）にお願いする作業の書き方（2026-09-02 追加・最優先）

先生はエンジニアではない。**先生に何かの操作をお願いするときは、すべて「中学生でもわかる手取り足取り」で書く。** 短く済ませようとして専門家向けの書き方に戻さない。

1. **1回のメッセージでお願いする作業は、原則1つだけ。** 複数あるときは「今日はこれだけ」と最初に言い、残りは `docs/for-owner/` の手順書に分けて置く。
2. **手順は番号つきで、1行に1つの動作。** 次を毎回書く。
   - どのアプリ・どのページを開くか（URLはそのまま貼る）
   - 画面のどこを押すか（「左上の▼」「右側の青いボタン」など位置と、ボタンに書いてある文字をそのまま）
   - 何を入力する・貼り付けるか（コピーする文字列はそのまま書く）
   - 押した後に**何が表示されれば成功か**
   - **失敗したときに何をするか**（多くは「出た文字をコピーして貼ってください。私が調べます」でよい）
3. **コマンドは、このアプリの「Run」ボタンで押せる形で渡す。** bashのコードブロックに、1ブロック1コマンド。中身の説明はいらない。「Runを押すと何が起きるか」「終わったら何と言えばよいか」を書く。
4. **専門用語（英語・IT用語）には、すぐ横に一言で意味を添える。** 例: push（＝作った変更を公開用のサーバーへ送ること）、noindex（＝検索結果に出さない設定）。
5. **先生に頼むのは、本当に先生しかできないことだけ。** ログインが必要な操作、法律・医療・お金の判断、本人確認。ツールで代行できることは先に代行し、権限で止まったときは「何が止まったか」「先生が代わりにやる最短の方法」を書く。
6. **「〜してください」で終わらせない。** 迷いそうな場所は、画面に出る文字（メニュー名・ボタン名）を書き、終わった後の合図（「押した」と返す、など）まで決める。
7. 先生向けの手順書は `docs/for-owner/` に置き、`docs/for-owner/README.md` から探せるようにする。チャットでは「今日やること1つ」と手順書の場所だけを伝える。

## 参照

- サイト管理の手順・安全ルール: `README.md`、`AGENTS.md`、`docs/codex-quickstart.md`
- コンテンツ制作のルール: `content/README.md`
- 先生向け手順書: `docs/for-owner/README.md`
