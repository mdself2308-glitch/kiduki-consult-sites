# T6 総合検証レポート

作業日: 2026-07-28

## 結論

問い合わせ導線の実装と自動検証は完了した。静的トップから新フォームへ到達し、
正常送信時だけサンクスページへ遷移し、Flamingoへ保存される。旧URLは全て
`/contact/`へ301着地する。

Phase 1の形式的な完了判定には、Codexから対象メールボックスとGoogle
Analytics管理画面へアクセスできないため、末尾の「院長確認が必要な項目」が残る。

## PC・スマホ表示と通し試験

| 項目 | 結果 |
|---|---|
| PC 1280px 静的トップ | H1・CTA・レイアウト正常、横方向の見切れなし |
| スマホ 390px 静的トップ | H1・CTAが画面内、`scrollWidth=390` |
| PC 1280px `/contact/` | フォーム1件、横方向の見切れなし |
| スマホ 390px `/contact/` | 入力文字16px、送信ボタン幅358px、`scrollWidth=390` |
| スマホCTA | 静的トップから`/contact/`へ到達 |
| 正常送信 | `/contact/thanks/`へ遷移 |
| サンクス | 確定文言、`noindex, follow`、390pxで見切れなし |

スクリーンショット:

- `reports/t6-2026-07-28/pc-top.jpg`
- `reports/t6-2026-07-28/mobile-top.jpg`
- `reports/t6-2026-07-28/pc-contact.jpg`
- `reports/t6-2026-07-28/mobile-contact.jpg`
- `reports/t6-2026-07-28/mobile-thanks.jpg`

スマホ確認は390pxブラウザ表示による検証であり、物理スマートフォン実機の最終確認は
院長確認として残す。

## フォーム

- 必須項目未入力で送信を停止し、
  「入力内容に問題があります。確認して再度お試しください。」を表示
- 会社名・相談内容・氏名・メールの未入力箇所へ
  「必須項目に入力してください。」を表示
- 不正メールアドレスで
  「正しいメールアドレスを入力してください。」を表示
- WP Armourのハニーポット項目を保持したまま正常送信に成功
- T6最終送信をFlamingo受信メッセージID 1762として保存
- 会社名、業種、従業員数、相談種別、内容、氏名、役職、メール、電話、同意、
  ハニーポット関連のフィールドキーを確認（入力値はログへ表示していない）
- テスト送信後のFlamingo公開件数は2件

通知メール2宛先と自動返信は設定済みで、Contact Form 7はメール送信成功イベントを
返してサンクスページへ遷移した。T6最終送信後、指定Gmailへの受信トレイ着地を
院長が確認した。

## GA4

T6で、サンクスページ判定の不具合により`contact-thanks.js`が読み込まれていない
ことを検出した。判定を固定ページID 1743・親ID 1741・slug `thanks`の一致へ修正し、
子テーマをdry-run・バックアップ後に再配備した。

- Site Kit Analytics 4: active / connected
- property、web data stream、計測ID `G-JQFWB6XG2E`: 設定済み
- Site Kitタグ出力: 有効
- W3TC結合後の公開JSに`generate_lead`とセッション内の二重送信防止処理を確認
- サンクスページは`noindex, follow`

Site Kitはセットアップ時のAnalytics権限が不足しており、key events一覧の読取が
403、`setupComplete=false`。Googleアカウントで再認可し、
`generate_lead`のキーイベント登録とリアルタイム受信を管理画面で確認する必要がある。

## メール認証とDNS

2026-07-28の権威DNS再確認:

- SPF TXT: 既存のXServer送信要件を満たすレコードを維持
- DKIM TXT: `default._domainkey`に公開鍵あり
- DMARC TXT: `v=DMARC1; p=none; rua=mailto:info@kdkconslt-sngyouijm.com;`
- MX: `kdkconslt-sngyouijm.com`

T2AではGmailとMicrosoft 365の受信トレイ着地を院長確認済み。受信メール詳細の
`spf=pass`、`dkim=pass`、`dmarc=pass`は受信側で確認する。

## 旧URL

| URL | 結果 |
|---|---|
| `reserve.*`ルート | 301 → `/contact/` |
| `reserve.*`任意パス+クエリ | 301 → `/contact/` |
| WordPress `/question/` | 301 → `/contact/`、`x-redirect-by: KIDUKI` |
| `/contact/` | 200 |
| `/contact/thanks/` | 200 / noindex |

WordPressのサーバーキャッシュに更新前メニューHTMLが残るページでも、旧
`/question/`は301で新フォームへ到達する。WordPressデータベース内の旧URLは0件。

## タイトル・description

- 静的トップtitle:
  `働く人の睡眠に特化した産業医事務所|東京KIDUKIコンサルティング産業医事務所`
- H1:
  `眠りから、職場の安全と生産性をつくる。`
- descriptionは`docs/copy/site-copy.md`の確定稿と一致
- ローカルと本番は85,972 bytes、SHA-256
  `b2125ef6e49ab12bb6fc934db7831692c54987308bbab412f07b99f977fe51db`
  で完全一致

## 表示速度

静的トップは変更前85,262 bytesから変更後85,972 bytesで、増分710 bytes
（約0.83%）。画像・外部ライブラリの追加はない。

同一時点で各3回計測:

| URL | TTFB | 総時間 | HTML |
|---|---:|---:|---:|
| 静的トップ | 0.298–0.319秒 | 0.391–0.413秒 | 85,972 bytes |
| `/contact/` | 0.040–0.435秒 | 0.066–0.814秒 | 290,309 bytes |

`/contact/`は新設ページのため変更前ページが存在せず、前後比較は非該当。

## 自動テスト

- `npm run verify`: 成功
- `npm run verify:static`: 成功
- `npm run verify:live-match`: 成功
- 子テーマ4ファイル: 本番SHA-256とローカルが一致
- `functions.php`: 本番ページが200で動作し、対象JSを出力
- ローカル環境にPHP CLIがないため、ローカル`php -l`は未実施

## 院長確認が必要な項目

1. T6最終テストメールについて、通知2宛先と自動返信を区別して確認
   （指定Gmailへの受信トレイ着地は確認済み）
2. 受信メール詳細で`spf=pass`、`dkim=pass`、`dmarc=pass`を確認
3. Site KitをGoogle Analytics権限付きで再認可し、`generate_lead`を
   キーイベントとして登録してリアルタイム受信を確認
4. 物理スマートフォンでトップ→フォーム→送信→サンクスを1回確認
