# 未処理項目チェックリスト

最終更新: 2026-07-31

> 事務所サイト（kdkconslt-sngyouijm.com / consult.）と Casetra
> （casetra.jp / consult.casetra.jp）の横断リスト。
> 実装済みのものはここに書かない。**人の操作が要るもの**と、
> **判断待ちのもの**だけを置く。

## 院長の操作が必要（GA4）

管理画面でしか設定できないため、こちらでは実施できない。
`generate_lead` は 2026-07-31 に発火を実測済み。GA4 の一覧に出るまで
数時間〜24時間かかるので、翌日以降に作業する。

- [ ] 事務所プロパティ（411006656）→ 管理 → イベント → 「最近のイベント」
      タブ → `generate_lead` の☆をオンにする
- [ ] Casetra プロパティ（518084661）で同じく `generate_lead` の☆をオン
- [ ] 両プロパティで `purchase` と `ads_conversion___1` の☆を**外す**
      （一度も発生しておらず、コンバージョン0のまま並んでレポートが読みにくい）
- [ ] `cta_click` `consult_booking_click` には☆を付けない
      （途中経過であってコンバージョンではない。付けると数字が水増しされる）
- [ ] GA4 アカウント名が「東京KIDUKIコンサルティング産業医事務所」のまま。
      管理 → アカウント → アカウントの詳細 から改名する
      （プロパティ名と Cal.com のチーム名は変更済み）

- [ ] Site Kit の Search Console データが取れるか、ブラウザで確認する。
      アプリケーションパスワード経由では `authenticated: false` となり
      接続状態を外から判別できなかった（403 の原因が権限不足か接続方法かは
      未確定）。Site Kit ダッシュボードで Search Console のカードにエラーや
      「権限を付与」ボタンが出ていれば権限不足、正常に数字が出ていれば問題なし
- [ ] AdSense が「Complete setup」のまま。事務所サイトに広告を出さないなら
      接続を切る（産業医事務所のページに広告枠があると信頼を損なう）

## 院長の確認が必要（Casetra 予約マッピング）

`kiduki-consult-api/config/eventTypeMapping.v1.json` と Cal.com の
イベントタイプを突き合わせた結果（2026-07-31 実測）。

- [ ] `insured_work_support` … マッピング定義はあるが Cal.com に該当枠が無い。
      両立支援の予約経路を作ったつもりで抜けているなら要対応
- [ ] 定義に無い13スラッグの扱い。予約されると UNKNOWN 扱いになり案件が
      自動生成されない。うち `intro-15-online` `intro15` は契約前の初回相談
      なので UNKNOWN で正しい。残り（`consult-online-30` `consult-urgent-online`
      `doctor-interview` `patrol` `committee` 等）が実運用で使われているなら、
      定義を足すか運用から外すかの判断が要る

## テストデータの削除

こちらの検証で作ったもの。内容を確認のうえ削除する。

- [ ] 事務所 Flamingo … 「テスト送信 動作確認用ダミー株式会社」2件
- [ ] Casetra 管理画面「Casetra 問い合わせ」… 1件
- [ ] メール … `kdkconsult.sngyijm@gmail.com` に計4通

## 判断待ち（着手していない）

- [ ] 睡眠研修の価格。2026-07-31 時点で保留。サイト上は全サービス
      「お問い合わせ」で統一している
- [ ] 旧ロゴ画像4点（メディア 1616 / 1618 / 1620 / 1621）。ファイル名に旧名称が
      残るが参照はゼロ。削除は破壊的なので未着手
- [ ] Emanon のアクセントカラーが旧設定（`#91c9a4`）のまま。ヘッダーの検索
      ボタンとモバイルのフローティングボタンがサイトの差し色（`#3f7a5e`）と
      合っていない

## 事業設計（産業衛生アウトソーシング / Casetra）

2026-07-31 の戦略議論より。着手前に設計・法務確認が要るもの。

- [ ] 契約の2本立て設計。医療契約（事務所/医師個人 ↔ 企業）と
      運用・SaaS契約（MedSelf ↔ 企業）を分離する。売却時に SaaS の
      ARR を汚さないため。責任分界の書き分けが必要
- [ ] 受託契約の責任設計。就業上の措置の決定権は事業者に残る旨を明記
      （安衛法の建付けどおり）／賠償上限（受託料の12か月分等）／
      証跡提供義務をこちらの義務として明記
- [ ] 保険の手当。医師賠償責任保険の適用範囲確認＋受託業務向けの
      賠償責任保険（情報漏洩含む）
- [ ] 「丸ごと」の法的な穴2つを商品説明に明記する。
      衛生管理者は社内選任義務（外注不可・事務局代行まで）／
      労基署提出書類の作成・提出代行は社労士独占業務の可能性
      → 社労士提携か「下書き支援＋提出は企業」の建付け
- [ ] プラットフォーム段階（他の医師を乗せる）前に、有料職業紹介の
      許認可要否を弁護士確認。「医療契約は直接締結・MedSelf は運用のみ」
      の建付けで回避できるかが論点
- [ ] オンライン面接指導の要件充足を契約に組み込む。年1回の職場訪問
      ＋巡視記録を契約条項に。「選任されていない医師の訪問が要件③の
      巡視に当たるか」は所轄労働局に照会
- [ ] BPO の工数/顧客を初月から計測する仕組み（売却時に「ソフトウェアが
      運転するサービス」であることを示す数字になる）
- [ ] ストレスチェックの Casetra 組込み設計。調査票（職業性ストレス簡易
      調査票57項目）と判定ロジックは厚労省公開物。要設計は3点：
      結果の本人同意ゲート（同意なく事業者に見せない区画）／実施者体制
      （医師＋提携保健師）／集団分析。**50人未満への義務化が2028-04-01
      施行で確定**（2026-06-10官報）。施行前に自前チャネルを持つ
- [ ] 健診は実施を作らない・奪わない。既存健診機関はそのままに、
      予約調整・結果回収・データ化・事後措置だけを取る運用設計
- [ ] 役割分離ルールの文書化。自分が産業医（または判断層）を務める企業の
      従業員を、原則として自院の患者にしない。受診勧奨は常に複数選択肢。
      例外は本人の自発選択＋関係開示＋文書化

## 記事（本体作業）

- [ ] 事務所コラム … 34本中、睡眠テーマは2本。最終投稿 2025-03-25（493日前）。
      題材が読者向け（モチベーション・疲労回復等）で、産業医を選ぶ
      人事・総務の検索語と噛み合っていない
- [ ] Casetra ブログ … 5本、すべて 2026-01。題材の方向は正しい
