# T0 調査・安全網レポート

調査日: 2026-07-28

> 秘密情報の値は本書およびGit管理下に記録しない。

## T0-1 本番構成とWordPressの棚卸し

- 公開トップはWordPressではなく `consult/index.html`。GitHubの `main` からAzure Static Web Appsへデプロイされる
- `https://kdkconslt-sngyouijm.com/` は静的トップへリダイレクトされる
- WordPressの管理/APIオリジンは `https://kdkconslt-sngyouijm.com`
- WordPress側フロントページ（ID 18、slug `home`）は公開トップとして使われていない
- WordPress 7.0.2、PHP 7.4.33、Emanon Premium 1.7.5、パーマリンク `/%postname%/`
- 調査時点で有効プラグインは26件。WordPressコア、親テーマ、プラグイン本体は直接編集しない
- `main` へのpushは2つのAzure Static Web Appsワークフローを起動するため、毎回、差分提示と明示承認を要する

## T0-2 旧Azure問い合わせフォームのメール送信資産

- 送信サービスは **Azure Communication Services Email（ACS Email）**。SendGridおよびMicrosoft 365ではない
- 送信元は独自ドメインではなく、**Azure既定ドメイン（`.azurecomm.net`）**
- 認証情報はクライアントHTMLや本リポジトリではなく、旧Azure Functionのサーバー側アプリ設定に置く接続文字列方式だった
- 旧Function、ACS Email、Cosmos DBを含むリソースグループは2026-05-10に削除済み。接続文字列/APIキー等の現物は現在存在せず、復元不能
- Git管理下に利用可能なAPIキー、接続文字列、パスワードはない。今後の秘密情報は `.env` または実行時の認証ストアだけで扱う
- 現在取得できる過去送信データ・送信履歴・過去リードはない。リード保存先だったCosmos DBも削除済みで、削除前の実データ件数や内容は確認不能
- 結論: 旧送信資産は流用せず、T2AでXServer上に新しい送信基盤を作る

## T0-3 現行問い合わせ導線の確認

- 静的トップから `reserve.kdkconslt-sngyouijm.com` への入口はHTTP 200で表示される
- 送信先だった旧Azure FunctionのホストはDNS解決不能
- API送信失敗時にもブラウザの `localStorage` へ保存して成功表示するため、利用者には完了したように見えるが、リードはサーバーへ保存・通知されない
- WordPress `/question/` にフォームはなく、旧Azure相談ページへのリンクだけがある
- 直通メールのフォールバックはない
- 結論: 2026-05-10以降、問い合わせ取りこぼしが起き得る状態。v2の順序どおり導線の止血と再構築を最優先とする

## T0-4 バックアップと復旧手段

- DBバックアップ: `/Users/dmmac/Downloads/2026-07-28_11-40-11_S73LGVBX02_DBDUMP.tar`
  - SHA-256: `266efdb285f760b45117e48e545f4b17d95d91330149a5567ddf8e95a18d9c5a`
- WordPressファイル一式: `/Users/dmmac/Downloads/2026-07-28_11-38-32_NX3LGVAG01_FILE-WPPLUGIN.tar`
  - SHA-256: `948e8785ed2a97a753c2c866248da812c13a77ab627cc5df5918b5e5facc10fb`
- 両ファイルともGit管理外、権限 `0600`、SHA-256検証済み
- 子テーマの表示差分が見つかった場合は、WordPress管理画面の「外観 → テーマ」で Emanon Premium を再有効化する
- BackWPupが、過去の復元作業に由来する残存ファイルと平文DB認証情報の可能性を警告している。削除対象はT0-R2で一覧提示し、承認後に削除する

## T0-R1 子テーマの本番有効化

実施日: 2026-07-28

### 実施結果

1. 有効化前に `theme_mods_emanon-premium` を `theme_mods_kiduki-child` へ複製
2. 複製直後の双方のシリアライズ値のSHA-256が一致
   - `b44344fa71385ee351ed2cac7ed1b441c6c41a75a4f40eafff9f5f44f73f0fd1`
3. 複製時の割り当て:
   - テーマ設定内メニュー位置: 9
   - メニュー割り当て済み位置: 3
   - 稼働ウィジェット: 54
4. `kiduki-child` 1.0.0をアップロードし、本番で有効化
5. 有効化後も公開REST APIで確認できるメニュー割り当て3件と稼働ウィジェット54件が維持された
6. `/office/`、`/question/`、`/privacy-policy/` をPC・モバイルで確認。タイトル、H1、ナビゲーション、画像件数に子テーマ有効化起因の差分はなく、リンク切れ画像も0件
7. モバイルの横方向の見切れは有効化前から同じ状態であり、今回の変更による回帰ではない
8. `npm run verify` と `npm run verify:static` は成功
9. 差分がなかったため親テーマへのロールバックは実施していない

複製にだけ使用した一時Code Snippetsコードは停止・削除した。管理画面の有効一覧およびゴミ箱には残っておらず、実行中の一時コードはない。

### 表示証跡

- [PC変更前: office（1440px）](screenshots/t0-r1/before-pc-office-1440.png)
- [PC変更後: office（1280px）](screenshots/t0-r1/after-pc-office-1280.png)
- [モバイル変更前: question（390px）](screenshots/t0-r1/before-mobile-question-390.png)
- [モバイル変更後: question（390px）](screenshots/t0-r1/after-mobile-question-390.png)

PCの前後画像は取得時のブラウザ実効幅が異なるためピクセル単位の比較には使わず、同一構成要素の表示確認に使用した。モバイルは同一幅で前後比較した。

### 成果物

- `wp-content/themes/kiduki-child/style.css`
- `wp-content/themes/kiduki-child/functions.php`
- `scripts/clone-theme-mods.mjs`
- 配布ZIP（Git管理外）: `/Users/dmmac/Downloads/kiduki-child-1.0.0.zip`
  - SHA-256: `176563043cf934e32fd480ac0843c92717609db703f327ba4ac9ca2056d1d39d`

### 現在の状態

- 本番の有効テーマ: `kiduki-child`
- `main` へのpush: 未実施
- T0-R2: 完了

## T0-R2 BackWPup復元残存物の整理

実施日: 2026-07-28

### 安全網

- T0-4記載のDB・WordPressファイル一式バックアップが存在し、権限`0600`であることを削除直前に再確認
- 削除対象3ファイルの内容と空ディレクトリ情報を、Git除外済み`backups/`へ対象限定バックアップとして追加保存
- 対象限定バックアップも権限`0600`、SHA-256検証済み。内容には復元用データが含まれるためGit・文書・画面には出さない
- dry runで対象が承認内容と一致し、`uploads/`が空であることを確認してから適用

### 削除した項目

- `wp-content/uploads/backwpup-restore/restore.dat`
- `wp-content/uploads/backwpup-restore/restore.dat.bkp`
- `wp-content/uploads/backwpup-restore/restore.log`
- `wp-content/uploads/backwpup-restore/uploads/`（空ディレクトリ）

### 存置した項目

- `wp-content/uploads/backwpup-restore/.htaccess`
- `wp-content/uploads/backwpup-restore/.donotbackup`
- `wp-content/uploads/backwpup-restore/index.php`

### 削除後の確認

- 読み取り専用の再棚卸しで、削除対象4項目がすべて不存在、存置3ファイルがすべて存在することを確認
- 稼働中のBackWPupは5.7.5
- 同版の`StaleRestoreFilesDetector`は、`restore.dat.bkp`の存在、または`uploads/`・`extract/`内の非dot項目を警告条件としている
- 削除後は`restore.dat.bkp`が不存在、`uploads/`が不存在、`extract/`も不存在。BackWPup自身の`StaleRestoreFilesDetector::has_files()`を読み取りで実行し、`false`を確認
- これらは復元処理の作業ファイルであり、通常のバックアップジョブでは再生成されない。新たな復元処理を開始した場合だけ再生成対象になる
- 調査・削除に使った一時Code Snippetsコードは停止・削除済み
