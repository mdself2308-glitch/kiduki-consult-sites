# AGENTS.md — このリポジトリでの作業ルール(Codex用)

## 参照文書(作業前に必ず読む)
- `docs/phase2-instructions.md` — **現在の作業指示書 v1.0**(実行スペック。Phase 2 の唯一の参照先)
- `docs/rebrand-strategy.md` — 事業戦略・ポジショニング(背景理解と実装判断の根拠)
- `docs/copy/` — 確定文言のマスター(site-copy.md/form-emails.md)
- `docs/phase1-instructions.md` — Phase 1 指示書(**完了済み**。環境事実の一次資料として参照)
- `docs/t0-report-2026-07-28.md` — T0調査レポート(環境事実の一次資料)

## 商品設計ドキュメント(商品に関わる実装の前に読む)
- `docs/sleep-training-design.html` — 睡眠研修パッケージ設計書(90分構成・アンケート・実施報告書様式)
- `docs/shift-design-rubric.html` — 交代勤務 勤務設計レビュー ルーブリック(判定基準と根拠)
- `docs/shift-report-template.html` — 同 報告書テンプレート

## 進め方(Phase 2)
- タスクは phase2-instructions.md §2 の実行順序に従う: **R0 → R1〜R5 → R6〜R7 → R8〜R11 → R12**
- 掲載する商品は**3本＋リンク2本で確定済み**(同 §0-11)。SAS対策プログラムと勤務設計レビューは
  実績ができるまでトップに掲げない
- 健康経営について書く際は、**認定要件と調査票を混同しない**(同 §0-11b)。
  令和8年度の変更は「案」の段階のため「新設される方針が示されています」と表現する

## 進め方
- タスクは指示書§2の実行順序に従う: **T0-R → T2A → S1 → T2B → T4 → T1 → T3 → T5 → T6**
- 1タスク = 1コミット。コミットメッセージは「T2B: 問い合わせフォーム新設」の形式
- 各タスク完了時に、変更内容の要約・確認手順・スクリーンショット(取得可能な場合)を報告し、**承認を得てから次のタスクに進む**(勝手に先へ進まない)
- 指示書§3の承認ゲートに該当する操作は、実行前に必ず明示承認を得る

## 絶対禁止
- **明示承認のない `main` へのpush**(両Azure Static Web Appsがデプロイされるため。毎回差分提示→承認)
- 親テーマ・プラグイン本体・WordPressコアの直接編集(カスタマイズはすべて子テーマ内)
- 指示書指定以外のプラグイン追加/既存ページ・記事・画像の削除
- 確定文言(`docs/copy/` 配下)の改変 — 改善案がある場合は実装せず「提案」として報告する
- **無承認でのDNS変更**。DNS変更は毎回「変更前後の差分一覧を提示→承認→XServer API経由で適用」の順を厳守。Azure SWAのカスタムドメインを支えるレコードには一切触れない
- サーバーパネル・レジストラのログイン情報の受領、ブラウザ自動操作によるログイン試行
- APIキー・接続文字列・パスワード類のコミット・画面表示・ログ出力(`.env`(gitignore済み)のみで扱い、在り処だけREADMEに記載)
- 「睡眠専門医」という語の使用(肩書きは指示書の統一表記のみ)

## 安全網
- 破壊的変更の前に必ずバックアップ(DB+ファイル一式)を取得し、保存先を報告
- 迷ったら止まって質問する。推測で進めない
- 問い合わせ動線(`/contact/`)に関わる変更は、指示書T6の検証チェックリストを全項目通過するまで「完了」と報告しない
- XServer APIの操作対象は `kdkconslt-sngyouijm.com` と当該サーバーアカウントに限定。他ドメイン・他サイト・契約/支払い情報には触れない
